import { isNull } from 'drizzle-orm'
import { members } from '../db/schema.ts'

/**
 * **지금 이 학생회의 사람.**
 *
 * 내보낸 사람의 줄은 표에 남는다(`members.leftAt`가 그 까닭을 적었다). 남은 줄이
 * 명단·조직도·고르는 목록·권한에 다시 나타나지 않게 하는 것이 이 조건 하나다.
 *
 * **쓰는 자리와 쓰지 않는 자리가 갈린다.**
 *
 * - **사람을 세거나 늘어놓는 조회** — 건다. 나간 사람은 지금의 사람이 아니다.
 * - **권한을 판정하는 조회** — 건다. 안 걸면 내보낸 사람이 그대로 들어온다.
 * - **id로 이어 붙여 이름을 붙이는 조회** — 걸지 않는다. 걸면 그 사람이 쓴 문서와
 *   올린 요청이 이름을 잃고 '알 수 없음'이 된다. 기록은 그 사람이 있던 때의 것이다.
 *
 * 자리마다 이 갈림을 지키는지는 `left-member.test.ts`가 나간 사람을 심어 두고
 * 진짜 조회를 불러 재 본다.
 */
export const stillHere = isNull(members.leftAt)
