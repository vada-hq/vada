import type { Handlers } from '../deps.ts'

// 재정(FIN-*). **아직 비어 있다** — 자리를 미리 열어 둔 것이다.
//
// 영역을 나란히 붙일 때 부딪히는 곳은 답이 아니라 **모으는 줄**이다(`index.ts`).
// 그래서 그 줄을 먼저 넣고 이 파일을 빈 채로 둔다. 답을 더하는 사람은 이 파일만
// 고치면 되고, 다른 영역과 같은 줄을 만지지 않는다.
//
// 재정은 **예산 위에 선다.** 예산을 정하는 화면이 명세에 없어(백로그 '결정 대기')
// 금액의 바탕이 되는 자리는 아직 못 짓는다. 지금 짓는 것은 **이미 일어난 것을
// 세는 자리**다 — 결재된 것·발주된 것·증빙이 붙은 것.

export const financeHandlers: Handlers = {}
