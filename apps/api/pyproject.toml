[project]
name = "vada-api"
version = "0.1.0"
description = "VADA API — FastAPI 모듈형 모놀리스"
requires-python = ">=3.13,<3.14"
dependencies = [
    "alembic>=1.15",
    "aws-lambda-powertools[tracer]>=3.31.1",
    "fastapi>=0.139",
    "mangum>=0.21.0",
    "psycopg[binary]>=3.2",
    "sqlalchemy>=2.0",
    "uvicorn[standard]>=0.34",
]

[dependency-groups]
dev = [
    "pytest>=9",
    "httpx>=0.28",
    "ruff>=0.15",
    "pyright>=1.1.411",
    "testcontainers[postgres]>=4.10",
    "pglast>=7",
    "jsonschema>=4.26",
    "schemathesis>=4.24.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vada_api"]

# ── Ruff: 넓게 select, 좁게 ignore (ADR: 린트·포맷) ──
[tool.ruff]
target-version = "py313"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E", "F", "I", "UP", "B", "SIM",  # 코어
    "S",                               # 보안(bandit) — 에이전트 코드 기본
    "RUF", "C4", "RET", "PTH", "FURB",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101"]  # 테스트의 assert 허용

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
# 개발용 스크립트도 테스트한다. 도는지 확인하지 않은 스크립트는 없는 것과 같다.
pythonpath = ["."]
markers = [
    "postgres: requires a disposable PostgreSQL database or Docker daemon",
]
