import type { Served } from './area'

/**
 * 운영 공간 그 자체(OPS-00 · OPS-CAL-01). **아직 비어 있다** — 자리를 미리 열어 둔 것이다.
 *
 * **회의와 가른다.** 회의는 `meetings.ts`가 든다. 여기 오는 것은 여러 표를 가로질러
 * 세는 것이다.
 */
export const ops: Served = {
  reads: [],
  writes: [],
}
