import type { Served } from './area'

/**
 * 재정(FIN-*). **아직 비어 있다** — 자리를 미리 열어 둔 것이다.
 *
 * 재정은 **예산 위에 선다.** 예산을 정하는 화면이 명세에 없어 금액의 바탕이 되는
 * 자리는 아직 못 짓는다(백로그 '결정 대기'). 먼저 오는 것은 **이미 일어난 것을 세는
 * 자리**다 — 결재된 것·발주된 것·증빙이 붙은 것.
 */
export const finance: Served = {
  reads: [],
  writes: [],
}
