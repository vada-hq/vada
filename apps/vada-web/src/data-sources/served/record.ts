import type { Served } from './area'

/**
 * 기록(REC-*). **아직 비어 있다** — 자리를 미리 열어 둔 것이다.
 *
 * 발행된 아카이브의 본문은 여기 오지 않는다. 발행이 무엇인지 아직 안 정했다.
 */
export const record: Served = {
  reads: [],
  writes: [],
}
