/**
 * 주소가 화면을 고르는 데 쓰는 이름들.
 *
 * **계약 밖의 이름이라 여기 있다.** 화면 여든넷의 id는 명세에서 오지만(`screens/`),
 * 이 둘은 아니다 — 와이어프레임이 **이미 들어온 사람만** 그렸기 때문에 로그인 화면이
 * 없고, '앱을 열면 어디부터인가'도 그림이 답하지 않는다.
 *
 * **한 자리에 둔다.** 앱의 길잡이(`App.tsx`)와 로그인 화면이 둘 다 이 이름을 아는데,
 * 각자 들고 있으면 한쪽만 고쳐도 아무 오류가 안 난다 — 로그인은 되는데 엉뚱한 데로
 * 가고, 그것이 조용하다. 실제로 그랬다.
 *
 * 그림이 생기면 이 파일이 사라지고 그 id가 `screens/`에서 온다.
 */

/** 로그인 화면. */
export const SIGN_IN = 'SIGN-IN'

/** 앱을 열면 여기부터. 로그인하고 나서 가는 곳도 여기다. */
export const FIRST_SCREEN = 'ONB-01'
