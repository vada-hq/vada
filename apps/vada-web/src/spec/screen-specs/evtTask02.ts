// 자동 생성: npm run generate:screen-runtime. 직접 수정하지 않는다.
import screenJson from '../../../../../specs/figma/vada-wireframe/screens/EVT-TASK-02/screen.json'
import { asScreenSpec } from '../screen-guard'

export const evtTask02 = asScreenSpec(screenJson, 'EVT-TASK-02')
