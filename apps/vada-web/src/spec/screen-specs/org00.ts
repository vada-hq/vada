// 자동 생성: npm run generate:screen-runtime. 직접 수정하지 않는다.
import screenJson from '../../../../../specs/figma/vada-wireframe/screens/ORG-00/screen.json'
import { asScreenSpec } from '../screen-guard'

export const org00 = asScreenSpec(screenJson, 'ORG-00')
