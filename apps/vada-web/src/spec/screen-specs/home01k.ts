// 자동 생성: npm run generate:screen-runtime. 직접 수정하지 않는다.
import screenJson from '../../../../../specs/figma/vada-wireframe/screens/HOME-01K/screen.json'
import { asScreenSpec } from '../screen-guard'

export const home01k = asScreenSpec(screenJson, 'HOME-01K')
