// 자동 생성: npm run generate:screen-runtime. 직접 수정하지 않는다.
import screenJson from '../../../../../specs/figma/vada-wireframe/screens/EXT-02C/screen.json'
import { asScreenSpec } from '../screen-guard'

export const ext02c = asScreenSpec(screenJson, 'EXT-02C')
