{
  "name": "figma-spec-v2",
  "private": true,
  "description": "저장소 루트에서 전체 검사를 일괄 실행하기 위한 스크립트 모음. 각 앱은 자기 package.json과 node_modules를 그대로 유지한다(워크스페이스로 묶지 않는다).",
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "validate": "node apps/spec-service/src/validate-specs.mjs",
    "e2e": "npm --prefix apps/vada-web run e2e",
    "build": "npm --prefix apps/vada-web run build",
    "check": "npm run test && npm run validate && npm run e2e && npm run build"
  }
}
