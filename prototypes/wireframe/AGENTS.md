# 와이어프레임 프로토타입 지침

- 이 디렉터리는 화면 탐색과 데모를 위한 참고 앱이다. 제품 구현은 `apps/web`, 실행 계약은 `contracts/`가 정본이다.
- 권한·상태·필드 의미를 이 코드만 보고 확정하지 않는다. 관련 슬라이스의 계약 기준선을 먼저 읽는다.
- 프로토타입 동작을 변경하면 `pnpm --filter @vada/wireframe build`를 실행한다.
- 배포 설정의 `.openai/hosting.json` 프로젝트 ID를 새로 만들거나 임의로 바꾸지 않는다.
