# vada-wireframe 원본 해석

이 wireframe의 `figma.raw.json`·`figma.design.json`을 읽을 때 필요한 캡처 특성과 화면별 특이사항이다. wireframe과 무관한 제품(vada) 구현 관례는 `docs/decisions/vada-conventions.md`를 따른다. (2026-08-17)

## 캡처 특성

- **스케일 0.875**: 수치 대부분이 통상 웹 값의 0.875배다(448=512×0.875, fontSize 12.25=14×0.875, 5.25=6×0.875). 구현은 값을 0.875로 나눠 환산한 뒤 표준 스케일로 스냅한다.
- 환산이 정수로 떨어지지 않는 값(예: 입력 높이 33.5→38.29)은 구성 요소 합(패딩+행간+테두리)으로 역산해 표준 조합으로 정한다.
- **폰트 "Inter"는 아티팩트**: 한글 글리프가 없어 실제로는 폴백으로 렌더된 것이다. 폰트명은 무시하고 크기·굵기 위계만 신뢰한다.
- **기준 뷰포트는 데스크톱 1288×740 한 장**이며 모바일 레이아웃 근거가 없다. 카드 폭 448(환산 512).

## 색 토큰 매핑

Tailwind v4 팔레트와 일치하는 값은 토큰으로 매핑한다.

| hex | 토큰 | 용례 |
| --- | --- | --- |
| #155DFC | blue-600 | 로고, 진행 pill, 주 버튼, 안내 상자 텍스트 |
| #1447E6 | blue-700 | 선택된 선택지 버튼 텍스트 |
| #2B7FFF | blue-500 | 선택된 선택지 버튼 테두리 |
| #DBEAFE | blue-100 | 안내 상자 테두리 |
| #EFF6FF | blue-50 | 선택된 선택지 버튼 배경, 안내 상자 배경 |
| #4A5565 | gray-600 | 선택지 버튼 텍스트, 묶음 제목 |
| #101828 | gray-900 | 제목, 로고 텍스트 |
| #1E2939 | gray-800 | 입력 값 텍스트 |
| #364153 | gray-700 | 활성 필드 라벨 |
| #6A7282 | gray-500 | 부제, 섹션 라벨 |
| #99A1AF | gray-400 | 비활성 라벨·placeholder, 활성 아이콘, 안내문 |
| #D1D5DC | gray-300 | 활성 입력 테두리, 비활성 아이콘 |
| #E5E7EB | gray-200 | 카드 테두리, 비활성 입력 테두리, 진행 pill(비활성) |
| #F9FAFB | gray-50 | 페이지 배경, 비활성 입력 배경 |
| #FB2C36 | red-500 | 필수 표시 * |

루트 프레임의 #F8F9FA는 팔레트 밖 값이며 안쪽 컨테이너(gray-50)에 가려 보이지 않으므로 무시한다.

## 표준 밖 값과 스냅 결과

÷0.875 환산이 표준 스케일로 떨어지지 않은 값들과 실제 적용한 스냅이다(0.875 가설의 반례 기록).

| wireframe 값 | ÷0.875 | 적용 |
| --- | --- | --- |
| fontSize 11 / lineHeight 16.5 (섹션 라벨·안내문) | 12.57 / 18.86 | text-xs(12/16) |
| fontSize 10 / lineHeight 15 (ONB-02 배지) | 11.43 / 17.1 | text-xs(12) |
| 카드 cornerRadius 9.25 | 10.57 | rounded-xl(12) |
| 입력 높이 33.5 | 38.29 | 구성 합(py-2 + text-sm 행간 20 + border 2 = 38)으로 자연 결정 |
| 카드 총폭 520 | 594.3 | 콘텐츠 512(max-w-lg 상당) + padding 40×2 + border 1×2 = 594 |
| ORG-02 부서 카드 161 | 184 | 안내 텍스트(147→168)가 컨테이너(138)를 넘친다. Figma 텍스트 자동 크기 아티팩트이므로 텍스트 실폭 기준 192로 잡는다 |

## 화면별 특이사항

### ONB-01

- 벡터 11개 식별: Search ×3, ChevronDown ×3, ArrowRight ×1. stroke 1.167은 lucide 기본 2px의 14px 환산이며 구현 크기는 16px이다.
- 현재 학년 `Dropdown`(7:75)은 자식 없는 448×20 빈 프레임이다. 다른 입력과 동일한 스타일로 구현한다.
- placeholder 텍스트가 값 색(#1E2939)으로 그려져 있다. 원본 결함으로 보고 전역 관례(placeholder gray-400)를 적용한다.
- 비활성 select(7:57, 7:68)의 시각: 배경 gray-50, 테두리 gray-200, 라벨·placeholder gray-400, 아이콘 gray-300. 활성 select(7:46): 배경 white, 테두리 gray-300, 아이콘 gray-400. 디자인에 있는 사실이므로 관례 6번(disabled 배경 gray-100)보다 우선한다.

### ONB-02

- 벡터 11개 식별: Plus(14:113, 20px·stroke 1.458=lucide 2px의 20px 환산), ExternalLink(14:127, 20px), ArrowRight ×2(카드 우측, 16px), ArrowLeft(14:147, 14px·stroke 1.021).
- 옵션 카드: 테두리 gray-200, 아이콘 타일 40px 배경 gray-100(#F3F4F6), 배지 배경 #FFEDD4=orange-100·텍스트 #CA3500=orange-700.
- 뒤로 가기(14:146)는 카드가 아니라 blue-600 텍스트 링크다. 카드/링크 구분은 흐름 순서(뒤로 이동=링크)로 판별한다.
- 진행 pill은 완료 단계까지 파란색으로 채운다(2/2에서 두 개 모두 파랑).

### ORG-01

- 벡터 11개 식별: Search ×2, ChevronDown ×3, ArrowLeft(14:227), ArrowRight(14:235). ONB-01과 같은 계열이다.
- **헤더에 Vada 로고가 없다**(14:155). 좌측은 eyebrow(14:158) + 제목(14:160)이고 우측만 ONB 화면과 같은 진행 표시다. 헤더 좌측 구성은 화면마다 다르므로 진행 표시(FlowProgress)만 공통이다.
- 학생회 유형(14:170)은 드롭다운이 아니라 **3열 그리드의 선택지 버튼 4개**다. 그리드 gap 7→8, 버튼 py 7→8·px 10.5→12·radius 3.5→4. 선택됨(14:173): 배경 blue-50·테두리 blue-500·텍스트 blue-700.
- 대표 범위 묶음(14:179)은 배경 gray-50·radius 5.25→6·padding 14→16·gap 10.5→12의 상자다. ONB-01의 섹션(배경 없는 제목만)과 같은 계급이지만 설명문과 배경이 더 있다.
- 안내 상자(14:222): 배경 blue-50·테두리 blue-100·radius 3.5→4·padding 10.5→12, 텍스트 10.5→12 blue-600.
- 하단(14:225)은 좌우 양끝 배치다. 좌측 `이전`(14:226)은 흰 배경·gray-300 테두리, 우측은 주 버튼(14:233, 내용 폭)과 그 아래 우측 정렬 안내문(14:239)이다. ONB-01의 주 버튼은 카드 폭 전체였으므로 폭은 화면마다 다르다.
- 학생회명 placeholder(14:212)가 값 색 #1E2939로 그려져 있다. ONB-01과 같은 원본 결함이며 전역 관례(gray-400)를 적용한다.
