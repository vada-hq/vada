{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "state-scopes.schema.json",
  "title": "Wireframe state scope catalog",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "scopes"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "scopes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "description",
          "lifetime",
          "clearOn"
        ],
        "properties": {
          "key": {
            "type": "string",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"
          },
          "description": {
            "type": "string",
            "minLength": 1
          },
          "lifetime": {
            "const": "flow"
          },
          "clearOn": {
            "description": "스코프를 제거하는 수명 이벤트. complete/cancel은 버튼이 낸다 — 보내고 나서(action.onSuccess.scopeEvent)거나 떠나면서(action.scopeEvent)다. 떠나는 쪽이 없던 동안 cancel은 선언만 있고 낼 방법이 없었다.",
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "enum": [
                "complete",
                "cancel"
              ]
            }
          }
        }
      }
    }
  }
}
