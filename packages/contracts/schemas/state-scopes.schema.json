{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "state-scopes.schema.json",
  "title": "Wireframe state scope catalog",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "scopes"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "scopes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "key",
          "description",
          "lifetime",
          "clearOn"
        ],
        "properties": {
          "key": {
            "type": "string",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$"
          },
          "description": {
            "type": "string",
            "minLength": 1
          },
          "lifetime": {
            "const": "flow"
          },
          "clearOn": {
            "description": "스코프를 제거하는 수명 이벤트. complete/cancel은 버튼 action의 scopeEvent로만 발생한다(설계 방향: element-types.md — 발생 지점 스키마는 첫 제출 버튼 등장 시 확정).",
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {
              "type": "string",
              "enum": [
                "complete",
                "cancel"
              ]
            }
          }
        }
      }
    }
  }
}
